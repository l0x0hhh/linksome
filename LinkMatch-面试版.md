# LinkMatch — AI 跨境服务智能撮合助手（MVP 产品方案）

> **一句话定位**：面向海外华人与出海企业的 AI 跨境服务撮合平台。用户用自然语言描述需求，AI 通过多轮 CoT 对话拆解意图，基于 RAG 匹配服务商，生成带推荐理由的服务方案。

---

## 一、产品架构图

### 系统架构

```
用户层（Web / 小程序 / WhatsApp）
        │
        ▼
┌───────────────────┐
│  API Gateway       │  认证 · 限流 · 路由
└───────┬───────────┘
        │
┌───────▼───────────┐
│  AI 对话引擎（核心） │
│  ┌─ 意图识别        │
│  ├─ CoT 推理链      │  → 核心壁垒：Prompt 工程 + 状态机设计
│  └─ 多轮对话状态机   │
└───────┬───────────┘
        │
   ┌────┴────┐
   ▼         ▼
┌──────┐ ┌──────┐
│ RAG  │ │ 匹配  │  向量检索 + 多维打分排序
│知识库│ │ 引擎  │
└──┬───┘ └──┬───┘
   │        │
   └──┬─────┘
      ▼
┌──────────────┐
│  订单转化层    │  需求 → 方案 → 询价 → 下单
└──────────────┘
```

### 信息架构（需求 → 交易全链路）

```
用户输入"我想在新加坡注册公司"
    │
    ▼ 意图识别 → 提取槽位 [地区:新加坡, 意图:公司注册]
    │
    ▼ 完整度检查 → 缺失 [预算, 公司类型] → 追问
    │
    ▼ RAG 检索 → Top 5 候选 → 多维打分 → Top 3
    │
    ▼ 方案生成 → 3家对比 + 推荐理由（可追溯到知识库原文）
    │
    ▼ 用户决策 → 下单 / 继续咨询 / 转人工
```

---

## 二、PRD（核心需求）

### 需求背景

跨境服务平台三大痛点：**信息过载**（几百家服务商平铺）、**匹配低效**（加微信逐个询价，耗时 3-7 天）、**信息不对称**（报价不透明，低质高价收割用户）。

### 用户画像（3 个典型）

| 画像 | 场景 | 痛点 |
|------|------|------|
| **留学生小李** | 英国创业注册公司 | 第一次接触，完全不知道流程和预算 |
| **SaaS 创始人老张** | 新加坡设控股公司 | 需要专业律所，不想和"办证中介"浪费时间 |
| **海外华人王姐** | 加拿大签证续签 | 英语不好，需要中文沟通的持牌顾问 |

### 功能优先级（版本节奏）

| 优先级 | 版本 | 核心交付 | 时间 |
|--------|------|---------|------|
| **P0** | V1.0 MVP | AI 对话采集 + RAG 匹配 + Top 3 推荐 + 50 家服务商知识库 | 4-6 周 |
| **P1** | V1.1 | 推荐结果对比增强 + 用户画像积累 + 中英双语 | 2-4 周 |
| **P2** | V1.2 | 在线下单支付 + 订单跟踪 + 服务商评分体系 | 2-4 周 |

---

## 三、AI 交互设计（差异化重点）

### Prompt 三层架构

| 层 | 作用 | 关键约束 |
|----|------|---------|
| **System** | 角色 + 规则 + 边界 | "你是跨境服务顾问，不编造信息，不给法律建议" |
| **Context** | RAG 检索结果 + 用户画像 + 历史对话 | 动态注入，驱动个性化推荐 |
| **Guard** | 安全护栏 | 禁止编造报价/服务商，敏感信息脱敏 |

### Agent 多轮对话状态机

```
START → 意图分类 → [跨境/闲聊/违规]
  ↓
跨境 → 槽位提取 → 完整度检查
  ├─ < 60% → 追问模式（每次最多 2 个问题，提供选项降低输入成本）
  ├─ 60-80% → 灵活判断
  └─ ≥ 80% → 触发 RAG → 生成推荐
                  ↓
            用户决策 → 下单 / 追问 / 换一批 / 转人工
```

**追问优先级**：服务类型 = 地区 > 预算 > 时效 > 公司类型 > 语言

### 安全与可信设计（网安背景加分）

**AI 幻觉防控（三层防线）**：
1. Prompt 层：强制要求推荐理由引用知识库原文，标注信息来源
2. RAG 层：检索为空时明确告知"暂未匹配"而非编造
3. 校验层：输出的服务商名/报价/资质需与知识库做文本相似度匹配（> 0.95），不通过则触发重生成或转人工

**服务商资质验证**：OCR 识别执照 → 政府/行业协会数据库 API 交叉核验（如 ACRA、IRS）→ 打标签（✅已认证 / ⚠️待审核 / 🔒平台担保）

**用户隐私边界**：四级数据分级（公开/对话/账户/敏感），原则是不收集身份证号等 PII，传输加密、用完即焚

---

## 四、数据指标与迭代路径

### 核心指标（北极星：月成功匹配交易数）

| 指标 | 定义 | MVP 目标 |
|------|------|---------|
| 需求拆解准确率 | AI 槽位提取 vs 人工标注一致率 | ≥ 85% |
| 推荐点击率 | 推荐卡片点击 / 展示 | ≥ 30% |
| 对话完成率 | 完整走完"需求→推荐"的用户比例 | ≥ 60% |
| 平均对话轮次 | 从需求到推荐的平均轮次 | ≤ 5 轮 |

### 上线后迭代路线

| 阶段 | 时间 | 核心动作 |
|------|------|---------|
| **止血期** | Week 1-2 | 全链路日志审查，日抽 20 条 Bad Case 标注修复 |
| **优化期** | Week 2-4 | A/B 测试追问策略/卡片样式，排序模型加特征（用户画像、响应率）|
| **冷启动** | Month 1-3 | 双边飞轮：用户画像个性化 + 定向邀请高质量服务商入驻 |
| **模型升级** | Month 3+ | Prompt-only → Fine-tuned 模型，RAG → 知识图谱 |

---

## 五、Web MVP 演示版（4 个页面）

| 页面 | 功能 | 技术实现 |
|------|------|---------|
| **AI 对话首页** | 自然语言入口，AI 多轮追问拆解需求 | React + SSE 流式对话 |
| **需求拆解页** | 动态展示 CoT 思维链，结构化需求标签 | Prompt 驱动 JSON 输出 |
| **服务商详情页** | AI 生成个性化推荐理由 + 信任标签 | RAG 检索 + LLM 生成 |
| **极简运营后台** | 上传服务商资料，更新 RAG 知识库 | 文件上传 + Embedding |

---

## 六、面试讲述策略

1. **开场**：跨境服务的本质是信任与匹配效率，"人找货"变"AI 造桥"
2. **架构**：为什么对话层和匹配层解耦 → 为了泛化到更多服务品类
3. **AI-Native**：展示用 Cursor/Claude Code 调 Prompt、改组件的实际过程
4. **差异化**：网安背景 → 服务商资质验证 + AI 幻觉防控 = 信任基础设施
5. **收尾**：LinkMatch 验证了 BAI 系统的理解——它不是聊天机器人，是服务电商化的操作系统
